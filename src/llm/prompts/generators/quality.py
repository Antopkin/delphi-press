"""Промпты для QualityGate (Stage 9).

Спека: docs-site/docs/generation/stages-6-9.md (§3).
Контракт: FactualCheckPrompt → CheckResult, StyleCheckPrompt → CheckResult.
"""

from __future__ import annotations

from src.llm.prompts.base import BasePrompt
from src.schemas.headline import CheckResult


class FactualCheckPrompt(BasePrompt):
    """Промпт для проверки фактической правдоподобности заголовка."""

    output_schema = CheckResult

    system_template = """\
Ты — факт-чекер. Оцени фактическую правдоподобность прогнозного заголовка.
НЕ оценивай, произойдёт ли событие — только внутреннюю непротиворечивость и соответствие известным фактам.

Оценка (1-5):
5: Полностью правдоподобно, фактов-противоречий не найдено
4: Мелкие неточности, не влияющие на смысл
3: Есть спорные утверждения, но принципиально не ошибочные
2: Содержит фактическую ошибку или серьёзное логическое противоречие
1: Явно абсурдный или невозможный сценарий"""

    user_template = """\
Оцени фактическую правдоподобность этого заголовка и первого абзаца.

Заголовок: {{ headline }}
Первый абзац: {{ first_paragraph }}

Контекст прогноза:
{{ prediction_context }}

Проверь:
1. Нет ли противоречий с общеизвестными фактами?
2. Корректны ли должности, названия, даты?
3. Логически непротиворечив ли сценарий?
4. Не является ли событие физически невозможным?

{{ schema_instruction }}"""


class StyleCheckPrompt(BasePrompt):
    """Промпт для проверки стилистической аутентичности заголовка."""

    output_schema = CheckResult

    system_template = """\
Ты — редактор с 15-летним стажем. Оцени, насколько заголовок и первый абзац
соответствуют стилю конкретного издания.

Оценка (1-5):
5: Неотличимо от настоящей публикации
4: Стиль в целом верный, мелкие шероховатости
3: Узнаваемый стиль, но с заметными отклонениями
2: Стиль не соответствует изданию
1: Совершенно не похоже на это издание"""

    user_template = """\
Оцени стилистическое соответствие заголовка изданию «{{ outlet_name }}».

Проверяемый заголовок: {{ headline }}
Проверяемый первый абзац: {{ first_paragraph }}

Примеры настоящих заголовков «{{ outlet_name }}»:
{% for h in sample_headlines %}
- {{ h }}
{% endfor %}

Параметры стиля издания:
- Средняя длина заголовка: {{ avg_headline_length }} символов
- Тональность: {{ emotional_tone }}
- Регистр: {{ capitalization }}
- Лексический регистр: {{ vocabulary_register }}

Проверь:
1. Типична ли лексика для этого издания?
2. Соответствуют ли синтаксические конструкции?
3. Подходит ли длина?
4. Верный ли тон?
5. Похоже ли это на реальную публикацию этого издания (smell test)?

{{ schema_instruction }}"""
